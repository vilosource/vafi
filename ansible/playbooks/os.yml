---
# OS configuration only — packages, hardening, timezone
#
# Usage:
#   cd ansible && ansible-playbook playbooks/os.yml -i inventory/dev.yml

- name: Base OS configuration
  hosts: k3s
  become: true
  roles:
    - role: ../roles/common
