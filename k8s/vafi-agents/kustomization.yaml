apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vafi-agents

resources:
  - sessions-pvc.yaml
  - cxdb-statefulset.yaml
  - cxdb-service.yaml
  - cxdb-ingress.yaml
  # secrets.yaml is a reference only - create secrets out-of-band with real values
  - executor-pool.yaml

commonLabels:
  app.kubernetes.io/part-of: vafi
  app.kubernetes.io/version: v0.1.0