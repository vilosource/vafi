---
# Placeholder secrets - NEVER commit actual values to git
# Apply actual values manually with: kubectl create secret generic vafi-secrets --from-literal=...
apiVersion: v1
kind: Secret
metadata:
  name: vafi-secrets
  namespace: vafi-agents
  labels:
    app.kubernetes.io/name: vafi-secrets
    app.kubernetes.io/part-of: vafi
type: Opaque
stringData:
  # Claude API credentials - replace with actual values
  ANTHROPIC_API_KEY: "PLACEHOLDER_ANTHROPIC_API_KEY"

  # vtf API token - replace with actual value
  VF_VTF_TOKEN: "PLACEHOLDER_VTF_TOKEN"